apiVersion: platform.example.com/v1alpha1
kind: XWebApp  # Composite Resource directo (sin Claim)
metadata:
  name: my-webapp-xr
  namespace: default  # XRs pueden estar en cualquier namespace
spec:
  parameters:
    appName: "demo-app"
    replicas: 3
    image: "nginx:1.21"
    namespace: "webapp-demo"
  compositionRef:
    name: webapp-composition